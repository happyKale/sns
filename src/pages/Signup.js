import React from "react";
import Header from '../shared/Header';

const Signup = (props) => {

    return (
        <React.Fragment>
            <Header/>
            여기가 회원가입이다.
        </React.Fragment>
    );
};

export default Signup;