import React from "react";
import styled from 'styled-components';
import { Grid, Img, Text } from '../elements';

const Post = (props) => {

    return(
        <React.Fragment>
            내가 포스트당!!!!
            <Grid>

            </Grid>
        </React.Fragment>
    );
};

export default Post;